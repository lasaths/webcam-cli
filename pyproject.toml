[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "webcam-cli"
dynamic = ["version"]
description = "MCP server for webcam access - capture photos and video for LLM agents"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Pavel Kirienko <pavel.kirienko@zubax.com>"}
]
keywords = ["mcp", "webcam", "camera", "llm", "agent", "model-context-protocol"]
classifiers = [
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Multimedia :: Video :: Capture",
]
dependencies = [
    "mcp>=1.6.0,<2.0.0",
    "opencv-python-headless>=4.8.0,<5.0.0",
]

[project.urls]
Homepage = "https://github.com/lasaths/webcam-cli"
Repository = "https://github.com/lasaths/webcam-cli"
Issues = "https://github.com/lasaths/webcam-cli/issues"

[dependency-groups]
dev = ["pytest", "pytest-asyncio", "pytest-mock", "ruff"]

[project.scripts]
webcam-cli = "webcam_cli.cli:main"

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["src/webcam_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = []
[tool.hatch.version]
path = "src/webcam_cli/__init__.py"
